name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # 👈 必须加这一行！

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build  # 输出到 ./dist

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v4  # 👈 升级到 v4（更稳定）
        with:
          deploy_key: ${{ secrets.ACTIONS_DEPLOY_KEY }}  # 👈 关键：用 deploy_key
          publish_dir: ./dist
          publish_branch: main  # 👈 必须指定 main
