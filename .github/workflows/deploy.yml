name: Deploy to GitHub Pages

on:
  push:
    branches: [ source ]  # 👈 监听 source 分支

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # 👈 必须保留：允许 GITHUB_TOKEN 写入仓库

    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "20"

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build  # 输出到 ./dist

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v4
        with:
          # 👇 主要修改在这里：
          github_token: ${{ secrets.GITHUB_TOKEN }}  # 使用 GitHub 自带的 Token
          publish_dir: ./dist
          publish_branch: main  # 👈 推送到 main 分支
